import React from 'react';

const EventSchedule = ({ schedule }) => {
  return (
    <div className="text-[15px] text-calendlyGrayText mt-0.5">
      {schedule}
    </div>
  );
};

export default EventSchedule;
