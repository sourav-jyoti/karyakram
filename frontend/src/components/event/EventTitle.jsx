import React from 'react';

const EventTitle = ({ title }) => {
  return (
    <h2 className="text-[19px] font-bold text-calendlyText leading-tight">
      {title}
    </h2>
  );
};

export default EventTitle;
